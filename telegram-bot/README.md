# Syntrix Telegram Bot

Полнофункциональный Telegram бот с админ-панелью для управления пользователями, депозитами и выводами.

## Возможности

### Для пользователей:
- `/start` - Регистрация и запуск WebApp
- `/balance` - Проверка баланса и истории транзакций
- Уведомления о депозитах и выводах
- Отображение статуса аккаунта (Active/Inactive/KYC Required/Blocked)

### Для администратора (ID: 503856039):
- `/admin` - Открыть админ-панель
- **Просмотр всех пользователей** с информацией:
  - @username
  - User ID
  - Status (Active/Inactive/KYC/Blocked)
  - Balance
  - Total Deposit
  - Total Withdrawal
- **Управление пользователями**:
  - Активация аккаунта
  - Деактивация
  - Установка KYC Required
  - Блокировка
- **Внесение баланса** пользователю вручную
- **Просмотр депозитов** (последние 10)
- **Просмотр выводов** (последние 10)
- **Уведомления**:
  - О новых регистрациях
  - О депозитах
  - О запросах на вывод

## Установка

### 1. Установить зависимости

```bash
cd telegram-bot
npm install
```

### 2. Создать бота в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Укажите имя: `Syntrix Bot`
4. Укажите username: `AiSyntrixTrade_bot` (или другой)
5. Скопируйте токен

### 3. Настроить окружение

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```
BOT_TOKEN=YOUR_BOT_TOKEN_FROM_BOTFATHER
ADMIN_ID=503856039
DATABASE_URL="file:./dev.db"
WEBAPP_URL=https://telegram-see-gichmain-hell32914s-projects.vercel.app

# Trading Cards Settings
CARDS_MIN_PER_DAY=4          # Минимальное количество карточек в день
CARDS_MAX_PER_DAY=16         # Максимальное количество карточек в день
CARDS_START_TIME=07:49       # Начало отправки (время Киева)
CARDS_END_TIME=22:30         # Конец отправки (время Киева)
```

### 4. Инициализировать базу данных

```bash
npm run db:push
```

### 5. Запустить бота

**Разработка (с авто-перезагрузкой):**
```bash
npm run dev
```

**Продакшн:**
```bash
npm run build
npm start
```

## База данных

Используется **SQLite** через Prisma ORM.

**Таблицы:**
- `User` - Пользователи
- `Deposit` - Депозиты
- `Withdrawal` - Выводы
- `Notification` - Уведомления

**Посмотреть базу данных визуально:**
```bash
npm run db:studio
```
Откроется Prisma Studio на http://localhost:5555

## Структура проекта

```
telegram-bot/
├── prisma/
│   └── schema.prisma      # Схема БД
├── src/
│   └── index.ts           # Основной код бота
├── .env                   # Конфигурация (не коммитится)
├── .env.example           # Пример конфигурации
├── package.json
└── tsconfig.json
```

## Команды админа

1. Отправьте `/admin` боту
2. Выберите действие:
   - **📊 All Users** - Список всех пользователей
   - **💰 Add Balance** - Добавить баланс пользователю
   - **📥 Deposits** - Просмотр депозитов
   - **📤 Withdrawals** - Просмотр выводов

### Управление пользователем

1. Нажмите "📊 All Users"
2. Выберите пользователя (кнопка "✏️ Manage")
3. Выберите действие:
   - ✅ **Activate** - Активировать аккаунт
   - ⏸ **Deactivate** - Деактивировать
   - 📋 **KYC Required** - Требуется KYC
   - 🚫 **Block** - Заблокировать
   - 💰 **Add Balance** - Добавить баланс

При изменении статуса пользователь получит уведомление в Telegram.

### Добавление баланса

1. Выберите пользователя
2. Нажмите "💰 Add Balance"
3. Введите сумму (например: `100`)
4. Пользователь получит уведомление о пополнении

## Интеграция с WebApp

Бот отправляет кнопку для запуска вашего React приложения:

```typescript
const keyboard = new InlineKeyboard()
  .webApp('🚀 Open Syntrix', WEBAPP_URL)
```

## Уведомления

Бот автоматически отправляет уведомления:

**Пользователям:**
- ✅ Аккаунт активирован
- ⏸ Аккаунт деактивирован
- 📋 Требуется KYC
- 🚫 Аккаунт заблокирован
- 💰 Пополнение баланса
- 📤 Запрос на вывод обработан

**Администратору:**
- 🆕 Новая регистрация
- 💰 Новый депозит
- 📤 Запрос на вывод

## Деплой на продакшн

### Вариант 1: VPS/Server

```bash
# Установить PM2
npm install -g pm2

# Собрать проект
npm run build

# Запустить с PM2
pm2 start dist/index.js --name syntrix-bot

# Автозапуск при перезагрузке
pm2 startup
pm2 save
```

### Вариант 2: Railway.app

1. Подключите GitHub репозиторий
2. Укажите Root Directory: `telegram-bot`
3. Добавьте переменные окружения
4. Railway автоматически соберёт и запустит бота

### Вариант 3: Vercel (Serverless)

Для serverless нужна другая архитектура через webhooks.

## Troubleshooting

**Бот не отвечает:**
- Проверьте `BOT_TOKEN` в `.env`
- Убедитесь что бот запущен: `npm run dev`

**Ошибки базы данных:**
```bash
rm -f prisma/dev.db
npm run db:push
```

**Админ панель не работает:**
- Проверьте `ADMIN_ID` в `.env`
- ID должен совпадать с вашим Telegram ID

## Лицензия

MIT
