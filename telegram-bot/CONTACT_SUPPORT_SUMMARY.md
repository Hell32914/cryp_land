# Глобальный Contact Support - Сводка изменений

## ✅ Что сделано

### 1. База данных
- **Удалены** поля из User: contactSupportActive, contactSupportBonusAmount, contactSupportTimerMinutes, contactSupportActivatedAt
- **Добавлено** поле в User: contactSupportSeen (Boolean) - отслеживает, видел ли пользователь модалку
- **Создана** новая таблица GlobalSettings с полями:
  - contactSupportEnabled (Boolean)
  - contactSupportBonusAmount (Float)
  - contactSupportTimerMinutes (Int)
  - contactSupportActivatedAt (DateTime)

### 2. API (telegram-bot/src/api.ts)
**Удален:**
- POST /api/admin/users/:telegramId/contact-support (старый per-user endpoint)

**Добавлены:**
- GET /api/settings/contact-support - получить глобальные настройки
- POST /api/admin/settings/contact-support - обновить глобальные настройки
- POST /api/admin/contact-support/show-to-active - сбросить флаг contactSupportSeen у всех юзеров
- POST /api/admin/contact-support/disable - отключить глобально
- POST /api/users/:telegramId/contact-support-seen - пометить пользователя как просмотревшего

### 3. Бот (telegram-bot/src/index.ts)
**Удалены:**
- Кнопки "📞 Contact Support" из управления отдельным пользователем
- Обработчик callback contact_support_{userId}
- Обработчики текстового ввода contact_support_bonus и contact_support_timer

**Добавлены:**
- Кнопка "📞 Global Contact Support" в главном админ меню
- Обработчик admin_global_contact_support - показывает текущие настройки
- Обработчик cs_configure - начало настройки (ввод бонуса и таймера)
- Обработчик cs_show_active - показать модалку всем активным юзерам
- Обработчик cs_disable - отключить глобально
- Обработчики текстового ввода cs_bonus_amount и cs_timer

### 4. Mini-App (telegram-app/src/App.tsx)
**Изменено:**
- useEffect теперь запрашивает глобальные настройки через API вместо проверки полей пользователя
- Модалка показывается если: settings.contactSupportEnabled=true И userData.contactSupportSeen=false
- При клике на SEND вызывается API для пометки как просмотренного

**Обновлено:**
- telegram-app/src/hooks/useUserData.ts - заменены старые поля на contactSupportSeen

## 📋 Как это работает

### Для админа:
1. Заходит в /admin
2. Нажимает "📞 Global Contact Support"
3. Видит текущий статус (Enabled/Disabled, сумму бонуса, таймер, оставшееся время)
4. Может:
   - **Configure Settings** - задать бонус и таймер, автоматически включается
   - **Show to Active Users** - сбросить флаг у всех юзеров → все увидят модалку при следующем запуске
   - **Disable Globally** - выключить показ модалки

### Для пользователя:
1. Открывает mini-app
2. Если contactSupportSeen=false И настройки включены → видит модалку
3. Видит бонус ($50 или другую сумму) и таймер обратного отсчета
4. Нажимает SEND → открывается @SyntrixSupport + флаг contactSupportSeen=true
5. При следующих запусках модалка не показывается

### Для новых пользователей:
- contactSupportSeen по умолчанию = false
- При первом запуске mini-app автоматически увидят модалку (если включено)

## 🚀 Деплой на сервер

**Важно:** На сервере в .env должно быть DATABASE_URL с postgresql://

1. Git pull
2. Изменить в schema.prisma: provider = "postgresql" (вместо sqlite)
3. npx prisma db push
4. node migrate-contact-support.cjs (опционально)
5. pm2 restart telegram-bot

Или использовать готовый скрипт:
```bash
bash deploy-contact-support.sh
```

## 📁 Файлы для деплоя

- `migrate-contact-support.cjs` - миграционный скрипт
- `deploy-contact-support.sh` - bash скрипт для автоматического деплоя
- `CONTACT_SUPPORT_DEPLOY.md` - подробная инструкция
- `CONTACT_SUPPORT_SUMMARY.md` - этот файл

## ⚠️ Важно

На локальной машине используется SQLite (для разработки), поэтому в schema.prisma стоит provider = "sqlite". 

Перед деплоем на сервер нужно убедиться, что:
1. В schema.prisma: provider = "postgresql"
2. В .env на сервере: DATABASE_URL="postgresql://..."

Или можно использовать условную логику в CI/CD для автоматической смены провайдера.
