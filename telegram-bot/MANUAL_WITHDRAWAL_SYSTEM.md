# Ручная система вывода средств

## Дата внедрения: 2 января 2026

## Описание изменений

Автоматический вывод средств через PayPal и OxaPay **временно отключен**. Теперь все выводы средств обрабатываются вручную администраторами.

---

## Как это работает

### Для пользователей:

1. **Пользователь создает заявку на вывод** в мини-приложении
   - Указывает сумму, валюту, адрес/email
   - Средства сразу резервируются (списываются с баланса)

2. **Заявка отправляется админам**
   - Пользователь получает уведомление: "Withdrawal Pending Approval"
   - Средства зарезервированы, баланс обновлен

3. **Ожидание обработки админом**
   - Админ получает уведомление с деталями
   - Админ одобряет или отклоняет заявку

4. **Результат:**
   - ✅ **Одобрено**: Админ вручную переводит средства → Отмечает заявку как "Completed"
   - ❌ **Отклонено**: Средства возвращаются на баланс пользователя

---

## Для администраторов

### Workflow обработки заявки:

1. **Получение уведомления**
   ```
   🔔 Withdrawal Request - Requires Approval
   
   👤 User: @username (ID: 123456789)
   💰 Amount: $50.00
   💳 Method: PAYPAL / OXAPAY
   💎 Currency: USD / USDT
   📍 Address: email@example.com / TXxx...xxx
   
   🆔 Withdrawal ID: 42
   ```
   
   **Кнопки:**
   - `✅ Approve & Process` - одобрить заявку
   - `❌ Reject` - отклонить заявку
   - `📊 Подробнее` - показать детали

2. **Одобрение заявки** (нажать "Approve & Process")
   
   Система:
   - Меняет статус на `APPROVED`
   - Уведомляет пользователя: "Withdrawal Approved - Admin will process your transfer manually"
   - Показывает админу инструкцию:
   
   ```
   ✅ APPROVED - Manual Transfer Required
   
   💳 Amount: $50.00
   📧 PayPal: user@email.com
   
   🔴 ACTION REQUIRED:
   1. Transfer $50.00 to user@email.com
   2. Click "Mark as Completed" below after transfer
   ```
   
   **Кнопки:**
   - `✅ Mark as Completed` - отметить как выполнено (после перевода)
   - `◀️ Back` - вернуться к списку

3. **Ручной перевод средств**
   
   **Для PayPal:**
   - Зайти на https://www.paypal.com/payouts/
   - Отправить деньги на указанный email
   - Вернуться в бот
   
   **Для Crypto (OxaPay):**
   - Зайти на https://oxapay.com/
   - Отправить криптовалюту на указанный адрес
   - Вернуться в бот

4. **Отметить как выполнено** (нажать "Mark as Completed")
   
   Система:
   - Меняет статус на `COMPLETED`
   - Уведомляет пользователя: "Withdrawal Completed! Funds have been transferred"
   - Заявка исчезает из списка pending

### Отклонение заявки

Если заявку нужно отклонить (нажать "❌ Reject"):
- Статус меняется на `FAILED`
- Средства автоматически **возвращаются** на баланс пользователя
- Пользователь получает уведомление о возврате средств

---

## Статусы заявок

| Статус | Описание | Действия админа |
|--------|----------|----------------|
| `PENDING` | Старые заявки (не должно быть новых) | Отклонить и попросить пересоздать |
| `PROCESSING` | Средства зарезервированы, ожидает одобрения | Одобрить или отклонить |
| `APPROVED` | Одобрено, ожидает ручного перевода | Перевести средства → Отметить как Completed |
| `COMPLETED` | Перевод выполнен | Ничего не требуется |
| `FAILED` | Отклонено админом | Средства вернулись пользователю |

---

## Панель администратора

### Просмотр заявок:

**Команда в боте:** `/admin` → `⏳ Pending Withdrawals`

Показывает все заявки со статусами: `PENDING`, `PROCESSING`, `APPROVED`

**Для каждой заявки показывается:**
- Username, сумма, валюта, адрес
- Статус с эмодзи:
  - ⏳ PENDING
  - 🔄 PROCESSING
  - ✅ APPROVED
- ID заявки, дата создания

**Кнопки действий:**
- Для `PENDING`/`PROCESSING`: `✅ Approve` и `❌ Reject`
- Для `APPROVED`: `✅ Complete` (отметить как выполнено)

---

## Изменения в коде

### 1. `telegram-bot/src/index.ts`

#### Изменен обработчик `approve_withdrawal_*`:

**PayPal:**
- ❌ **Удалено**: Автоматическая отправка через `createPayPalPayout()`
- ✅ **Добавлено**: Установка статуса `APPROVED`, уведомление админа о необходимости ручного перевода

**OxaPay:**
- ❌ **Удалено**: Автоматическая отправка через `createPayout()`
- ✅ **Добавлено**: Установка статуса `APPROVED`, уведомление админа о необходимости ручного перевода

#### Добавлен новый обработчик `complete_withdrawal_*`:

Позволяет админу отметить заявку как выполненную после ручного перевода:
- Меняет статус на `COMPLETED`
- Отправляет пользователю подтверждение
- Обновляет `txHash` на `MANUAL_TRANSFER_COMPLETED`

#### Обновлен обработчик `admin_pending_withdrawals`:

- Теперь показывает также заявки со статусом `APPROVED`
- Для `APPROVED` заявок отображается кнопка `✅ Complete`
- Для `PENDING`/`PROCESSING` - кнопки `✅ Approve` и `❌ Reject`

### 2. База данных

**Новые значения статуса:**
- `APPROVED` - заявка одобрена, ожидает ручного перевода

**Новые значения txHash:**
- `AWAITING_MANUAL_TRANSFER` - установлено при одобрении
- `MANUAL_TRANSFER_COMPLETED` - установлено после отметки "Completed"

Изменений в схеме Prisma не требуется (status - это String).

---

## Как вернуть автоматические выводы

Если в будущем нужно включить обратно автоматические выводы:

### Для PayPal:

В файле `telegram-bot/src/index.ts`, в обработчике `approve_withdrawal_*`, заменить блок:

```typescript
// PayPal payout - MANUAL PROCESSING ONLY
if (paymentMethod === 'PAYPAL') {
  // ... текущий код ...
}
```

На старый код (закомментирован в истории Git):

```typescript
// PayPal payout
if (paymentMethod === 'PAYPAL') {
  try {
    const { createPayPalPayout } = await import('./paypal.js')
    const payout = await createPayPalPayout({ ... })
    // ... автоматическая отправка ...
  } catch { ... }
}
```

### Для OxaPay:

Аналогично заменить блок с `MANUAL PROCESSING ONLY` на блок с `createPayout()`.

---

## Тестирование

### Проверка работы системы:

1. **Создание заявки:**
   - В мини-приложении запросить вывод средств
   - Проверить, что баланс уменьшился
   - Проверить уведомление пользователя

2. **Получение уведомления админом:**
   - Админ должен получить уведомление с кнопками
   - Проверить корректность данных (сумма, адрес, метод)

3. **Одобрение:**
   - Нажать `✅ Approve & Process`
   - Проверить инструкцию для ручного перевода
   - Проверить уведомление пользователя

4. **Завершение:**
   - После ручного перевода нажать `✅ Mark as Completed`
   - Проверить уведомление пользователя о завершении

5. **Отклонение:**
   - Создать тестовую заявку
   - Нажать `❌ Reject`
   - Проверить, что средства вернулись на баланс

---

## Поддержка

Если у пользователей возникают вопросы:
- Объяснить, что выводы теперь обрабатываются вручную для безопасности
- Среднее время обработки: от нескольких минут до нескольких часов
- Средства резервируются сразу при создании заявки
- Если заявка отклонена, средства возвращаются автоматически

---

## История изменений

- **2 января 2026**: Внедрена ручная система вывода средств
  - Отключен автоматический вывод через PayPal и OxaPay
  - Добавлен статус APPROVED
  - Добавлен обработчик complete_withdrawal
  - Обновлен интерфейс админ-панели
