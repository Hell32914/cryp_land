# PayPal автоматическое пополнение через Webhook

## Что изменилось

✅ **Убрана** кнопка "I paid — Confirm"  
✅ **Добавлен** PayPal Webhook для автоматического подтверждения платежей  
✅ **Добавлена** автоматическая проверка статуса на фронтенде  

Теперь пользователь:
1. Нажимает "Pay with PayPal"
2. Оплачивает на PayPal
3. Возвращается в приложение
4. Баланс обновляется автоматически (через webhook или авто-проверку)

---

## Настройка на сервере

### 1. Обновите код

```bash
cd /root/cryp_land
git pull
cd telegram-bot
npm install  # если были изменения в зависимостях
npm run build
pm2 restart syntrix
```

### 2. Настройте PayPal Webhook

#### 2.1. Зайдите в PayPal Developer Dashboard

1. Откройте: https://developer.paypal.com/dashboard/
2. Выберите свое приложение (то, от которого CLIENT_ID)
3. Перейдите в раздел **"Webhooks"**

#### 2.2. Добавьте Webhook

Нажмите **"Add Webhook"** и заполните:

**Webhook URL:**
```
https://api.syntrix.website/api/paypal-webhook
```

**Event types** (выберите эти события):
- ✅ `CHECKOUT.ORDER.APPROVED` - когда пользователь подтвердил оплату
- ✅ `PAYMENT.CAPTURE.COMPLETED` - когда платеж захвачен (backup)

**Опционально** (для дополнительного логирования):
- `CHECKOUT.ORDER.COMPLETED`
- `PAYMENT.CAPTURE.DENIED`  
- `PAYMENT.CAPTURE.PENDING`

Нажмите **"Save"**.

#### 2.3. Проверьте Webhook

После сохранения PayPal покажет **Webhook ID**. Сохраните его для справки.

Протестируйте webhook:
1. В PayPal Dashboard → Webhooks → выберите ваш webhook
2. Нажмите **"Simulate Events"**
3. Выберите событие `CHECKOUT.ORDER.APPROVED`
4. Нажмите **"Send"**

Проверьте логи сервера:
```bash
pm2 logs syntrix --lines 50 | grep -i webhook
```

Вы должны увидеть:
```
📨 PayPal Webhook received
Webhook event type: CHECKOUT.ORDER.APPROVED
```

### 3. Обновите фронтенд

#### Автоматический способ (рекомендуется):

```bash
cd /root/cryp_land
powershell ./apply-paypal-autopatch.ps1
# Или на Linux:
# bash apply-paypal-autopatch.sh
```

#### Ручной способ:

1. Откройте `telegram-app/src/App.tsx`
2. Найдите функцию `checkPayPalDepositStatus` - она уже добавлена
3. Найдите PayPal UI блок (строка ~1126)
4. Замените старый код:

```tsx
// УДАЛИТЕ ЭТО:
<Button onClick={() => window.open(depositPaymentUrl, '_blank')}>
  Continue to PayPal
</Button>
<Button onClick={handlePayPalDepositConfirm}>
  I paid — Confirm
</Button>
```

На новый:

```tsx
// НОВЫЙ КОД:
<Button
  onClick={async () => {
    window.open(depositPaymentUrl, '_blank')
    setTimeout(() => {
      let attempts = 0
      const maxAttempts = 20
      const checkInterval = setInterval(async () => {
        attempts++
        const completed = await checkPayPalDepositStatus(depositPaypalOrderId)
        if (completed || attempts >= maxAttempts) {
          clearInterval(checkInterval)
        }
      }, 6000)
    }, 5000)
  }}
>
  Pay with PayPal 👉
</Button>
<div className="flex items-center gap-2 p-3 bg-blue-500/10 rounded-lg">
  <span>ℹ️</span>
  <p className="text-xs">
    After payment, return here. Balance updates automatically.
  </p>
</div>
<Button onClick={async () => {
  const completed = await checkPayPalDepositStatus(depositPaypalOrderId)
  if (!completed) toast.info('⏳ Payment still processing...')
}}>
  🔄 Check Status Now
</Button>
```

#### Соберите и задеплойте:

```bash
cd telegram-app
npm run build

# Если используете Vercel/Netlify - они соберут автоматически после git push
# Если деплоите вручную - скопируйте dist/ на сервер
```

---

## Как это работает

### Основной flow (через Webhook):

1. **Пользователь создает депозит** → создается запись в БД со статусом `PENDING`
2. **Перенаправляется на PayPal** → оплачивает
3. **PayPal подтверждает платеж** → отправляет webhook `CHECKOUT.ORDER.APPROVED`
4. **Сервер получает webhook** → автоматически делает `capture` платежа
5. **Платеж захвачен** → обновляется баланс пользователя, отправляется уведомление
6. **Пользователь возвращается** → видит обновленный баланс

### Backup flow (если webhook не сработал):

1. **Пользователь возвращается** в приложение после оплаты
2. **Автоматически запускается** проверка статуса каждые 6 секунд (20 раз = 2 минуты)
3. **Фронтенд проверяет** API `/api/user/{id}/deposits`
4. **Если платеж completed** → показывает уведомление, обновляет баланс

### Ручная проверка:

- Пользователь может нажать **"Check Status Now"**
- Также можно обновить страницу — баланс обновится

---

## Тестирование

### 1. Локальное тестирование webhook

Для тестирования webhook локально используйте ngrok:

```bash
# Установите ngrok если нет
npm install -g ngrok

# Запустите туннель
ngrok http 3001

# Используйте URL от ngrok в PayPal Dashboard Webhook settings
# Например: https://abc123.ngrok.io/api/paypal-webhook
```

Затем сделайте тестовый платеж в sandbox.

### 2. Проверка в sandbox

1. Убедитесь, что `PAYPAL_ENV=sandbox` в `.env`
2. Создайте депозит через бота
3. На PayPal sandbox странице войдите с тестовым аккаунтом
4. Подтвердите платеж
5. Проверьте логи:

```bash
pm2 logs syntrix | grep -i paypal
```

Должно быть:
```
📨 PayPal Webhook received
✅ Order XXX approved, attempting auto-capture...
✅ Payment captured successfully
🎉 Deposit completed automatically for user 123456
```

### 3. Проверка в production

После переключения на `PAYPAL_ENV=live`:

1. Сделайте реальный тестовый платеж ($1)
2. Проверьте, что webhook срабатывает
3. Проверьте, что баланс обновляется автоматически
4. Проверьте уведомления пользователю и админам

---

## Troubleshooting

### Webhook не работает

**Проверьте:**

```bash
# 1. Webhook URL доступен извне
curl https://api.syntrix.website/api/paypal-webhook
# Должен вернуть 200 OK или ошибку (но не timeout)

# 2. Логи сервера
pm2 logs syntrix --lines 100 | grep webhook

# 3. PayPal Dashboard → Webhooks → ваш webhook → Recent deliveries
# Там будет история всех отправленных webhooks и их статусы
```

**Частые проблемы:**

- ❌ **404 Not Found** → проверьте URL, убедитесь что путь правильный
- ❌ **500 Server Error** → проверьте логи сервера, возможна ошибка в коде
- ❌ **Timeout** → сервер не отвечает, проверьте что он запущен и доступен
- ❌ **SSL Error** → убедитесь что используете HTTPS

### Автопроверка не работает

**Проверьте в браузере (Developer Tools → Console):**

```javascript
// Должна быть функция checkPayPalDepositStatus
// Проверьте, нет ли ошибок JavaScript
```

**Проверьте API endpoint:**

```bash
curl -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  https://api.syntrix.website/api/user/YOUR_TELEGRAM_ID/deposits
```

### Баланс не обновляется

**Проверьте логи:**

```bash
pm2 logs syntrix | grep -A 10 "Order.*approved"
```

**Проверьте БД:**

```bash
# Зайдите в Prisma Studio или проверьте через SQL
# Статус депозита должен быть COMPLETED
```

**Проверьте user balance:**

```bash
# В логах должно быть:
# "totalDeposit increment by X"
```

---

## Откат изменений

Если нужно вернуть старую логику с кнопкой "Confirm":

### Backend:

Закомментируйте webhook endpoint в `api.ts`:

```typescript
// app.post('/api/paypal-webhook', ... )
```

### Frontend:

Верните старый код:

```tsx
<Button onClick={() => window.open(depositPaymentUrl, '_blank')}>
  Continue to PayPal
</Button>
<Button onClick={handlePayPalDepositConfirm}>
  I paid — Confirm
</Button>
```

Соберите и задеплойте.

---

## Мониторинг

### Проверка работы webhook:

```bash
# Смотрите логи реального времени
pm2 logs syntrix --lines 0

# Фильтруйте только PayPal события
pm2 logs syntrix | grep -i paypal

# Статистика успешных депозитов
pm2 logs syntrix | grep "Deposit completed automatically" | wc -l
```

### PayPal Dashboard:

1. Зайдите в PayPal Developer Dashboard
2. Webhooks → ваш webhook
3. **Recent deliveries** покажет:
   - Какие события были отправлены
   - Статус ответа вашего сервера
   - Retry attempts (если были)

---

## Преимущества нового подхода

✅ **Лучший UX** - пользователю не нужно ничего подтверждать вручную  
✅ **Быстрее** - баланс обновляется сразу после оплаты через webhook  
✅ **Надежнее** - есть backup через автопроверку, если webhook не сработал  
✅ **Меньше ошибок** - пользователь не может нажать "Confirm" до оплаты  
✅ **Меньше поддержки** - меньше вопросов "я оплатил, но баланс не пришел"  

## Безопасность

Webhook endpoint открыт (не требует авторизации), но это безопасно потому что:

1. **Проверяется order ID** - создается только через наш API
2. **Capture запрос идет с нашего сервера** - используя наш access token
3. **PayPal подписывает webhooks** - можно добавить проверку подписи (опционально)
4. **Сумма валидируется** - capture не пройдет если сумма неверная

Для дополнительной защиты можно добавить проверку PayPal подписи webhook (см. документацию PayPal).

---

**Готово!** Теперь PayPal платежи работают автоматически без необходимости ручного подтверждения. 🎉
