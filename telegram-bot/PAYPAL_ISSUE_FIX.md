# Диагностика и исправление проблемы PayPal

## Проблема
Деньги с карты списались, но баланс в боте не пополнился и уведомление не пришло.

## Возможные причины
1. ❌ Webhook от PayPal не настроен или не работает
2. ❌ Webhook получен, но произошла ошибка при обработке
3. ❌ Платеж в статусе APPROVED, но не захвачен (captured)
4. ❌ Проблема с соединением между PayPal и вашим сервером

---

## 🔧 Шаг 1: Диагностика на сервере

Подключитесь к серверу:
```bash
ssh root@45.147.248.134
# Пароль: 8VkuQOl6e63lkWF
```

Загрузите файлы на сервер:
```bash
cd /root/cryp_land/telegram-bot

# Скачать новые файлы с репозитория
git pull

# Если файлы не появились, создайте вручную или скопируйте через scp
```

Запустите диагностику:
```bash
chmod +x diagnose-paypal.sh
./diagnose-paypal.sh
```

Эта команда покажет:
- ✅ Получает ли сервер webhooks от PayPal
- ✅ Какие депозиты есть в базе и их статус
- ✅ Настройки PayPal
- ✅ Работает ли webhook endpoint

---

## 🔧 Шаг 2: Проверка конкретного депозита

Проверьте последние депозиты PayPal:
```bash
node check-paypal-issue.cjs
```

Найдите ваш депозит и посмотрите:
- **Status**: должен быть COMPLETED (если PENDING - платеж не обработан)
- **PayPal Order ID**: ID заказа от PayPal (например, `7AB12345CD678901E`)
- **User Total Deposit**: текущий баланс пользователя

---

## 🔧 Шаг 3: Ручной захват платежа (если webhook не сработал)

Если депозит в статусе PENDING, используйте Order ID для ручного захвата:

```bash
node manual-paypal-capture.cjs <ORDER_ID>
```

Например:
```bash
node manual-paypal-capture.cjs 7AB12345CD678901E
```

Этот скрипт:
1. ✅ Найдет депозит в базе
2. ✅ Захватит платеж из PayPal
3. ✅ Обновит баланс пользователя
4. ✅ Пришлет уведомление в бот

---

## 🔧 Шаг 4: Проверка PayPal Webhook настроек

### 4.1. Проверьте, настроен ли webhook в PayPal

1. Зайдите в PayPal Developer Dashboard: https://developer.paypal.com/dashboard/
2. Выберите ваше приложение (то, от которого CLIENT_ID)
3. Перейдите в **Webhooks**
4. Проверьте, есть ли webhook с URL: `https://api.syntrix.website/api/paypal-webhook`

### 4.2. Если webhook не настроен:

Создайте webhook:
1. Нажмите **"Add Webhook"**
2. **Webhook URL**: `https://api.syntrix.website/api/paypal-webhook`
3. **Event types**: выберите:
   - ✅ `CHECKOUT.ORDER.APPROVED`
   - ✅ `PAYMENT.CAPTURE.COMPLETED`
4. Сохраните

### 4.3. Протестируйте webhook:

В PayPal Dashboard:
1. Выберите ваш webhook
2. Нажмите **"Simulate Events"**
3. Выберите `CHECKOUT.ORDER.APPROVED`
4. Нажмите **"Send"**

Проверьте логи на сервере:
```bash
pm2 logs syntrix --lines 20
```

Вы должны увидеть:
```
📨 PayPal Webhook received
Webhook event type: CHECKOUT.ORDER.APPROVED
```

---

## 🔧 Шаг 5: Проверка логов сервера

Посмотрите логи за последний час:
```bash
pm2 logs syntrix --lines 200 | grep -i "paypal\|webhook\|deposit"
```

Ищите:
- ✅ `PayPal Webhook received` - webhook получен
- ❌ `No pending deposit found` - депозит не найден в базе
- ❌ `Failed to auto-capture payment` - ошибка при захвате
- ✅ `Deposit completed automatically` - успешно обработано

---

## 🔧 Шаг 6: Проверка конкретного пользователя

Если знаете Telegram ID пользователя:
```bash
node check-user.cjs
```

(Отредактируйте файл и укажите нужный telegramId)

---

## 📋 Частые проблемы и решения

### Проблема 1: Webhook не приходит от PayPal
**Причина**: Webhook не настроен или неправильный URL

**Решение**:
1. Настройте webhook в PayPal Dashboard (см. Шаг 4)
2. Убедитесь, что URL правильный: `https://api.syntrix.website/api/paypal-webhook`
3. Протестируйте webhook через PayPal Dashboard

### Проблема 2: Webhook приходит, но депозит не найден
**Причина**: Order ID не совпадает или депозит не создан в базе

**Решение**:
1. Проверьте логи: `pm2 logs syntrix | grep "No pending deposit found"`
2. Проверьте базу: `node check-paypal-issue.cjs`
3. Если депозита нет - проблема при создании заказа

### Проблема 3: Депозит создан, но webhook не обработал его
**Причина**: Ошибка при обработке webhook

**Решение**:
1. Найдите Order ID в логах или базе
2. Используйте ручной захват: `node manual-paypal-capture.cjs <ORDER_ID>`

### Проблема 4: Платеж не может быть захвачен
**Причина**: Платеж истек или отменен в PayPal

**Решение**:
1. Проверьте статус в PayPal Dashboard → Orders
2. Если платеж отменен - создайте возврат пользователю
3. Если платеж завершен в PayPal, но не в базе - используйте ручной захват

---

## 🚀 Быстрое решение (если webhook не работает)

Если webhook не настроен или не работает:

```bash
# 1. Найдите все PENDING PayPal депозиты
node check-paypal-issue.cjs

# 2. Для каждого PENDING депозита выполните:
node manual-paypal-capture.cjs <ORDER_ID>

# 3. Настройте webhook для будущих платежей (см. Шаг 4)
```

---

## 📞 Если ничего не помогло

1. Соберите информацию:
   - Output от `diagnose-paypal.sh`
   - Output от `check-paypal-issue.cjs`
   - Order ID от PayPal
   - Telegram ID пользователя

2. Проверьте PayPal Dashboard:
   - Есть ли там платеж?
   - Какой его статус?
   - Когда он был создан?

3. Проверьте, работает ли API:
   ```bash
   curl https://api.syntrix.website/api/paypal-webhook
   ```

---

## ✅ После исправления

1. Перезапустите бот:
   ```bash
   pm2 restart syntrix
   ```

2. Протестируйте новый платеж:
   - Создайте тестовый депозит
   - Проверьте логи
   - Убедитесь, что webhook получен

3. Уведомите пользователя о зачислении средств (если использовали ручной захват)
